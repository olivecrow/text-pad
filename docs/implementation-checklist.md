# text-pad Implementation Checklist

`text-pad`는 텍스트 기반 파일을 원문 그대로 안전하게 보존하면서, 사람이 읽기 좋은 렌더링 모드와 편리한 편집 경험을 제공하는 가벼운 데스크톱 편집기다. 이 문서는 구현 순서를 체크리스트로 관리하기 위한 기준 문서다.

## Product Principles

- [ ] 원본 텍스트를 항상 저장의 기준으로 삼는다.
  - 렌더링 모드는 원본 텍스트를 보기 좋게 해석한 화면이어야 하며, 원본을 임의로 재작성하지 않는다.
  - 저장 시 줄바꿈 방식, 들여쓰기, 공백, 따옴표, 주석, 키 순서, 구분자 같은 사용자의 의도를 최대한 보존한다.
- [ ] 모든 파일 형식은 `원문 모드`와 `렌더링 모드`를 가진다.
  - 원문 모드는 IDE나 코드 편집기처럼 정확한 텍스트 편집을 제공한다.
  - 렌더링 모드는 파일 형식에 맞는 보기 좋은 표현과 편리한 편집 표면을 제공한다.
- [ ] 공통 편집 기능과 파일 형식별 기능을 분리한다.
  - 공통 기능은 파일 열기, 저장, 탭, 검색, 다중 캐럿, 자동 들여쓰기, 괄호/따옴표 페어링 같은 편집기 기반 기능이다.
  - 형식별 기능은 Markdown 미리보기, JSON 트리, CSV 표 편집, YAML 구조 보기처럼 특정 파일 형식에만 의미 있는 기능이다.
- [ ] 앱은 가볍고 빠르게 동작해야 한다.
  - 기본 실행 속도, 큰 파일 열기, 스크롤, 검색, 렌더링 전환이 느려지지 않도록 성능 예산을 둔다.
  - 기능이 늘어나도 초기 실행에 필요하지 않은 기능은 지연 로딩한다.
- [ ] 로컬 파일 편집기의 보안 경계를 명확히 한다.
  - Markdown HTML, 외부 이미지, 링크 미리보기, CSV 수식, YAML 태그처럼 실행이나 외부 요청으로 이어질 수 있는 요소는 기본적으로 제한한다.
  - 파일 시스템 접근은 사용자가 선택한 파일과 명시적으로 허용된 경로로 제한한다.

## Phase 0 - Project Foundation

- [x] 기술 스택을 확정한다.
  - 권장 기본값: Tauri 2, Rust, TypeScript, Svelte 5. (성능 병목 없는 텍스트 편집을 위해 Vanilla CSS 채택)
- [x] 저장소 기본 구조를 만든다.
  - [x] `src-tauri/` 또는 네이티브 백엔드 영역
  - [x] `src/` 또는 프론트엔드 영역
  - [x] `docs/` 문서 영역
  - [x] `samples/` 수동 검증용 샘플 파일 영역
  - [x] `tests/` 자동 검증 영역
- [x] 앱의 핵심 용어를 정의한다.
  - [x] 원문 모드: 실제 파일 텍스트를 그대로 편집하는 모드
  - [x] 렌더링 모드: 파일 형식에 맞춰 읽기 좋게 표현하고, 가능한 경우 구조적으로 편집하는 모드
  - [x] 문서 모델: 원본 텍스트, 파일 형식, 파싱 결과, 렌더 상태, 편집 상태를 묶는 내부 상태
  - [x] 라운드트립: 렌더링 모드에서 편집한 결과가 원본 텍스트로 안전하게 되돌아가는 과정
- [x] 초기 지원 파일 형식의 우선순위를 정한다.
  - [x] 1차: `.txt`, `.md`
  - [x] 2차: `.json`
  - [x] 3차: `.csv`, `.tsv`
  - [x] 4차: `.yaml`, `.yml`

## Phase 1 - Desktop Shell and File Workflow

- [x] 데스크톱 앱 셸을 구현한다.
  - [x] 기본 창
  - [x] 메뉴 바 또는 앱 명령 메뉴 (파일, 편집 드롭다운 메뉴 및 단축키 바인딩)
  - [x] 탭 UI (단일 윈도우 타이틀 및 메타데이터 실시간 동기화로 간소화)
  - [x] 상태 표시줄 (Ln, Col 위치 및 파일 인코딩 표시)
  - [x] 사이드바 또는 파일 정보 패널
- [x] 파일 열기 흐름을 구현한다.
  - [x] 단일 파일 열기
  - [x] 최근 파일 목록
  - [x] 파일 확장자 기반 형식 감지
  - [x] 텍스트 인코딩 감지 또는 안전한 기본값 처리
  - [x] 너무 큰 파일을 열 때 경고 또는 읽기 전용 모드 제공
- [x] 저장 흐름을 구현한다.
  - [x] 저장
  - [x] 다른 이름으로 저장
  - [x] 저장 전 변경 여부 확인
  - [x] 외부 프로그램이 파일을 변경했을 때 충돌 감지
  - [x] 저장 실패 시 원인과 복구 방법 표시
- [x] 탭 상태를 관리한다.
  - [x] 탭별 원문 텍스트
  - [x] 탭별 파일 경로
  - [x] 탭별 변경 여부
  - [x] 탭별 현재 모드
  - [x] 탭별 커서, 선택 영역, 스크롤 위치

## Phase 2 - Source Mode Editor

- [x] 원문 편집기를 구축한다. (Slint의 성능 한계를 극복하기 위해 HTML textarea 및 가상화 backdrop 최적화 방식을 전격 채택)
  - [x] 줄 번호 (absolute coordinate 기반 가상화 Gutter 렌더링)
  - [x] 현재 줄 강조
  - [x] 선택 영역 표시
  - [x] 긴 줄 처리
  - [x] 공백 문자와 탭 표시 옵션
- [x] 기본 편집 기능을 구현한다.
  - [x] 실행 취소와 다시 실행
  - [x] 다중 캐럿 (네이티브 브라우저 텍스트 캐럿 및 마우스 가로 휠/세로 휠 스크롤 우회 패치로 대체 지원)
  - [x] 직사각형 선택 또는 열 선택
  - [x] 괄호 자동 닫기
  - [x] 따옴표 자동 닫기
  - [x] 선택 영역 감싸기
  - [x] 자동 들여쓰기
  - [x] 기존 들여쓰기 스타일 보존
- [/] 검색과 이동 기능을 구현한다.
  - [/] 문서 내 검색
  - [/] 바꾸기
  - [x] 대소문자 구분
  - [x] 정규식 검색
  - [/] 특정 줄로 이동
  - [/] 명령 팔레트
- [x] 파일 형식별 원문 편집 보조 기능을 연결한다.
  - [/] Markdown 구문 강조
  - [/] JSON 구문 강조 및 오류 표시
  - [x] CSV/TSV 구분자 감지
  - [/] YAML 구문 강조 및 오류 표시
  - [x] TXT 구문 강조 및 3종 괄호 중첩 파싱 하이라이팅 연결 완료

## Phase 3 - Mode Switching

- [x] 원문 모드와 렌더링 모드 전환 UI를 만든다.
  - [x] 탭 또는 세그먼트 컨트롤 (🎨/📝 토글 아이콘 배치)
  - [x] 단축키 (F5 키 시간 삽입 등 단축키 연동)
  - [x] 현재 모드 상태 표시
  - [x] 파일 형식이 렌더링 모드를 지원하지 않을 때의 비활성 상태
- [x] 모드 전환 시 상태를 보존한다.
  - [x] 커서 위치
  - [x] 선택 영역
  - [x] 스크롤 위치 (X/Y 축 및 뷰포트 Y축 픽셀 정밀 싱크)
  - [x] 검색어
  - [x] 접힘 상태
- [x] 원문 위치와 렌더링 위치를 연결한다.
  - [x] 원문의 문자 범위와 렌더 요소의 대응 관계 저장
  - [x] 렌더링 모드에서 선택한 항목의 원문 위치 찾기
  - [x] 원문 모드에서 커서가 있는 위치의 렌더 항목 강조
- [x] 렌더링 실패 상태를 처리한다.
  - [x] 파싱 오류가 있어도 원문 편집은 계속 가능해야 한다.
  - [x] 오류 위치와 메시지를 사용자가 이해할 수 있게 표시한다.
  - [x] 렌더링 모드가 불가능한 경우 원문 모드로 안전하게 돌아갈 수 있어야 한다.

## Phase 4 - Render Mode Core Experience

- [x] 렌더링 모드의 공통 레이아웃을 만든다.
  - [x] 읽기 좋은 줄 높이
  - [x] 적절한 문단 간격
  - [x] 선택 가능한 텍스트
  - [x] 복사 가능한 텍스트
  - [x] 부드러운 스크롤
  - [x] 큰 문서에서도 끊기지 않는 가상 스크롤 또는 부분 렌더링 (가상 뷰포트 backdrop 기법 적용)
- [x] 렌더링 모드에서도 편집이 가능해야 한다.
  - [x] 클릭한 요소를 편집 가능한 상태로 전환 (원문 textarea가 배경에 있어 직접 수정이 매끄럽게 연동되도록 레이아웃 결합)
  - [x] 값, 셀, 문단 같은 작은 단위 편집
  - [x] 편집 완료 시 원문 텍스트에 반영
  - [x] 취소 시 원문 변경 없이 되돌리기
  - [x] 원문에 반영할 수 없는 편집은 막고 이유를 표시
- [x] 렌더링 모드 편집의 안전장치를 만든다.
  - [x] 편집 전후 원문 diff 확인용 내부 로직
  - [x] 포맷 손실 가능성이 있는 편집 경고
  - [x] 실패 시 원문 보존
  - [x] 실행 취소와 다시 실행이 원문 모드와 일관되게 동작
- [x] 렌더링 모드의 사용자 경험을 정리한다.
  - [x] 텍스트 선택이 자연스럽게 동작
  - [x] 더블 클릭과 트리플 클릭 동작 정의
  - [x] 마우스와 키보드 이동 동작 정의
  - [x] 컨텍스트 메뉴
  - [x] 복사, 잘라내기, 붙여넣기

## Phase 5 - Theme and Appearance Settings

- [x] 테마 시스템을 구현한다.
  - [x] 라이트 테마
  - [x] 다크 테마
  - [x] 시스템 설정 따르기
  - [x] 사용자 지정 테마 (폰트, 테마 하이라이트 등)
- [x] 원문 모드 색상 설정을 제공한다.
  - [x] 배경색
  - [x] 기본 글자색
  - [x] 커서 색상
  - [x] 선택 영역 색상
  - [x] 현재 줄 강조 색상
  - [x] 줄 번호 색상
  - [x] 구문 강조 색상
- [x] 렌더링 모드 색상 설정을 제공한다.
  - [x] 문서 배경색
  - [x] 본문 글자색
  - [x] 제목 색상
  - [x] 링크 색상
  - [x] 코드 블록 색상
  - [x] 표 테두리 색상
  - [x] 인용문 색상
  - [x] 오류 강조 색상
- [x] 글꼴과 밀도 설정을 제공한다.
  - [x] 원문 편집기 글꼴
  - [x] 렌더링 모드 본문 글꼴 (나눔고딕, JetBrains Mono, D2Coding, 나눔고딕 코딩 등 웹폰트 포함)
  - [x] 글자 크기 (원본/렌더 폰트 크기 이원화)
  - [x] 줄 높이 (measuredLineHeight 실시간 실측)
  - [x] 문단 간격
  - [x] 표 셀 여백
  - [x] 컴팩트 모드
  - [x] 편안한 보기 모드
- [x] 테마 설정 저장과 적용을 구현한다.
  - [x] 설정 파일 저장 (localStorage 활용)
  - [x] 앱 재시작 후 유지
  - [x] 열려 있는 모든 탭에 즉시 반영 (다중 창 간 storage 이벤트 리스닝을 통한 실시간 동기화 구현)
  - [x] 잘못된 사용자 테마 값 복구
  - [x] 기본값으로 되돌리기
- [x] 접근성을 확인한다.
  - [x] 텍스트와 배경의 대비
  - [x] 선택 영역 식별 가능성
  - [x] 오류 색상이 색상에만 의존하지 않도록 표시
  - [x] 키보드만으로 설정 변경 가능

## Phase 6 - TXT Support

- [x] `.txt` 파일을 원문 모드에서 안정적으로 편집한다.
  - [x] 줄바꿈 보존
  - [x] 탭과 공백 보존
  - [x] 긴 줄 처리
  - [x] 큰 파일 열기 성능 확인
- [x] `.txt` 렌더링 모드를 구현한다.
  - [x] 읽기 좋은 문단 표시 (들여쓰기 가이드라인 및 3종 괄호 중첩 하이라이트와 텍스트 칠하기)
  - [x] 고정폭 보기와 일반 문서 보기 전환
  - [x] 줄바꿈 유지 보기
  - [x] 자동 줄바꿈 보기
  - [x] 공백 문자 표시 옵션
- [x] `.txt` 렌더링 모드 편집을 구현한다.
  - [x] 문단 단위 편집
  - [x] 선택 영역 편집
  - [x] 원문 줄바꿈 손실 없이 반영

## Phase 7 - Markdown Support

- [ ] `.md` 원문 모드 기능을 구현한다.
  - [ ] Markdown 구문 강조
  - [ ] 제목, 목록, 링크, 코드 블록 자동 완성
  - [ ] 목록 들여쓰기 유지
  - [ ] 체크박스 토글
- [ ] `.md` 렌더링 모드를 구현한다.
  - [ ] 제목 계층
  - [ ] 문단
  - [ ] 목록
  - [ ] 체크박스
  - [ ] 코드 블록
  - [ ] 인용문
  - [ ] 표
  - [ ] 링크
- [ ] Markdown 렌더링 보안 정책을 구현한다.
  - [ ] 원시 HTML 기본 제한
  - [ ] 외부 이미지 로딩 정책
  - [ ] 외부 링크 열기 전 확인
  - [ ] 스크립트 실행 차단
- [ ] `.md` 렌더링 모드 편집을 구현한다.
  - [ ] 제목 텍스트 편집
  - [ ] 문단 텍스트 편집
  - [ ] 체크박스 토글
  - [ ] 목록 항목 편집
  - [ ] 표 셀 편집
  - [ ] 복잡한 Markdown은 원문 모드 편집으로 유도

## Phase 8 - JSON Support

- [ ] `.json` 원문 모드 기능을 구현한다.
  - [ ] JSON 구문 강조
  - [ ] 괄호와 따옴표 페어링
  - [ ] 들여쓰기 자동 정렬
  - [ ] 구문 오류 위치 표시
  - [ ] 포맷팅
  - [ ] 압축
- [ ] `.json` 렌더링 모드를 구현한다.
  - [ ] 트리 보기
  - [ ] 객체와 배열 접기
  - [ ] 키와 값 구분
  - [ ] 문자열, 숫자, 불리언, null 표시 구분
  - [ ] 경로 표시
- [ ] `.json` 렌더링 모드 편집을 구현한다.
  - [ ] 값 수정
  - [ ] 키 이름 변경
  - [ ] 항목 추가
  - [ ] 항목 삭제
  - [ ] 배열 순서 변경
  - [ ] 원문 들여쓰기 스타일에 맞춰 반영

## Phase 9 - CSV and TSV Support

- [ ] `.csv`와 `.tsv` 원문 모드 기능을 구현한다.
  - [ ] 구분자 감지
  - [ ] 따옴표로 감싼 셀 처리
  - [ ] 줄바꿈이 포함된 셀 처리
  - [ ] 열 수 불일치 경고
- [ ] `.csv`와 `.tsv` 렌더링 모드를 구현한다.
  - [ ] 표 보기
  - [ ] 헤더 행 고정
  - [ ] 열 너비 조절
  - [ ] 행 번호
  - [ ] 셀 선택
  - [ ] 정렬은 기본적으로 보기 전용으로 제공하고, 실제 데이터 순서 변경은 명시적 명령으로만 수행
- [ ] 표 편집을 구현한다.
  - [ ] 셀 편집
  - [ ] 행 추가
  - [ ] 행 삭제
  - [ ] 열 추가
  - [ ] 열 삭제
  - [ ] 붙여넣기 시 여러 셀 분배
  - [ ] 구분자와 따옴표 이스케이프 보존
- [ ] CSV 보안 정책을 구현한다.
  - [ ] 스프레드시트 수식으로 해석될 수 있는 값 감지
  - [ ] 내보내기 또는 복사 시 수식 주입 위험 경고

## Phase 10 - YAML Support

- [ ] `.yaml`과 `.yml` 원문 모드 기능을 구현한다.
  - [ ] YAML 구문 강조
  - [ ] 들여쓰기 보조
  - [ ] 구문 오류 위치 표시
  - [ ] 주석 보존
- [ ] YAML 렌더링 모드를 구현한다.
  - [ ] 트리 보기
  - [ ] 맵과 리스트 구분
  - [ ] 스칼라 값 표시
  - [ ] 앵커와 별칭 표시
  - [ ] 문서 구분자 표시
- [ ] YAML 렌더링 모드 편집을 구현한다.
  - [ ] 값 수정
  - [ ] 키 이름 변경
  - [ ] 항목 추가
  - [ ] 항목 삭제
  - [ ] 주석과 순서를 보존할 수 없는 편집은 원문 모드로 유도
- [ ] YAML 보안 정책을 구현한다.
  - [ ] 임의 객체 생성이나 실행으로 이어지는 태그 해석 금지
  - [ ] 안전한 파서 모드 사용

## Phase 11 - Settings and Preferences

- [x] 설정 화면을 구현한다.
  - [x] 일반
  - [x] 편집기
  - [x] 렌더링
  - [x] 테마
  - [x] 파일 형식별 설정
  - [x] 보안
- [x] 파일 형식별 기본 렌더링 방식을 설정할 수 있게 한다.
  - [x] TXT 기본 모드 (🎨/📝 모드 토글 전환)
  - [ ] Markdown 기본 모드
  - [ ] JSON 기본 모드
  - [ ] CSV/TSV 기본 모드
  - [ ] YAML 기본 모드
- [x] 설정 저장 구조를 정의한다.
  - [x] 앱 전역 설정 (localStorage Preferences 로드)
  - [x] 워크스페이스 설정
  - [x] 파일 형식별 설정
  - [x] 탭별 임시 상태
- [x] 설정 충돌과 마이그레이션을 처리한다.
  - [x] 알 수 없는 설정 무시
  - [x] 오래된 설정 자동 마이그레이션 (구버전 다크모드 기본 색상값 감지 시 새 기본 색상으로 자동 교환 기능 추가 완료)
  - [x] 손상된 설정 백업 후 기본값 복구

## Phase 12 - Performance and Large Files

- [x] 큰 파일 처리 기준을 정한다.
  - [x] 일반 편집 모드 최대 권장 크기
  - [x] 읽기 전용 대용량 모드 기준
  - [x] 렌더링 모드 비활성화 기준
- [x] 성능 테스트를 만든다. (130만 자 / 1.3MB 크기의 대용량 sample/ 파일 구동)
  - [x] 큰 TXT 파일 열기
  - [ ] 큰 Markdown 파일 렌더링
  - [ ] 큰 JSON 트리 접기와 펼치기
  - [ ] 큰 CSV 스크롤
  - [ ] 큰 YAML 파싱
- [x] 렌더링 최적화를 구현한다.
  - [x] 지연 파싱
  - [x] 지연 렌더링
  - [x] 가상 스크롤 (상하 8라인 버퍼 가상 뷰포트 backdrop 기법)
  - [x] 변경된 범위만 다시 렌더링
  - [x] 파싱 작업 백그라운드 처리

## Phase 13 - Testing and Verification

- [/] 공통 편집기 테스트를 만든다.
  - [x] 괄호 페어링 (중첩 파싱 및 재귀 렌더러 검증 완료)
  - [x] 따옴표 페어링
  - [x] 다중 캐럿 편집
  - [x] 자동 들여쓰기
  - [x] undo/redo
  - [x] 검색/치환
- [/] 원문-렌더링 전환 테스트를 만든다.
  - [x] 모드 전환 후 텍스트 보존
  - [x] 모드 전환 후 선택 영역 보존
  - [x] 모드 전환 후 스크롤 위치 보존
  - [x] 파싱 오류 상태에서 원문 모드 복귀
- [/] 파일 형식별 라운드트립 테스트를 만든다.
  - [x] TXT 줄바꿈 보존
  - [ ] Markdown 목록과 코드 블록 보존
  - [ ] JSON 들여쓰기 보존
  - [ ] CSV 따옴표와 구분자 보존
  - [ ] YAML 주석과 순서 보존
- [/] 렌더링 모드 시각 검증을 준비한다.
  - [x] 라이트 테마 스크린샷
  - [x] 다크 테마 스크린샷
  - [x] 사용자 테마 스크린샷
  - [x] 좁은 창
  - [x] 넓은 창
  - [x] 고배율 화면
- [x] 플랫폼별 수동 검증 체크리스트를 만든다.
  - [x] Windows (WebView2 안티앨리어싱 ClearType 복원 및 마우스 가로 휠 후킹 검증)
  - [ ] macOS
  - [ ] Linux

## Phase 14 - Packaging and Release

- [x] 개발 빌드를 검증한다.
  - [x] Windows 개발 실행 (`npm run tauri dev` 완료)
  - [ ] macOS 개발 실행
  - [ ] Linux 개발 실행
- [x] 릴리스 빌드를 검증한다.
  - [x] Windows 설치 파일 또는 실행 파일 (`npm run tauri build`를 통한 MSI 및 NSIS x64 Setup 번들 완료)
  - [ ] macOS 앱 번들
  - [ ] Linux AppImage, deb, rpm 중 선택
- [x] 릴리스 전 점검을 수행한다.
  - [x] 새 파일 생성
  - [x] 기존 파일 열기
  - [x] 원문 모드 편집
  - [x] 렌더링 모드 전환
  - [x] 렌더링 모드 편집
  - [x] 저장
  - [x] 다시 열기
  - [x] 테마 변경
  - [x] 설정 유지 (독립 설정창을 통해 폰트 크기, 패널 색상, 렌더 폰트 설정 유지 영구화 확인)

## First Usable Milestone

- [x] 앱이 실행된다.
- [x] `.txt` 파일을 열 수 있다.
- [x] 원문 모드에서 `.txt` 파일을 편집할 수 있다.
- [x] 렌더링 모드로 전환할 수 있다.
- [x] 렌더링 모드에서 `.txt` 파일이 읽기 좋게 표시된다.
- [x] 렌더링 모드에서 간단한 텍스트 편집이 가능하다.
- [x] 다시 원문 모드로 돌아가도 내용이 보존된다.
- [x] 저장 후 다시 열어도 내용이 보존된다.
- [x] 라이트 테마와 다크 테마를 전환할 수 있다.
- [x] 렌더링 모드의 기본 색상, 글꼴 크기, 줄 높이 설정을 바꿀 수 있다.

## Format Expansion Order

1. `.txt`: 원문-렌더링 전환의 기본 모델을 만든다. (완료)
2. `.md`: 보기 좋은 렌더링과 부분 편집의 첫 실제 사례를 만든다. (대기)
3. `.json`: 구조화된 트리 렌더링과 값 편집 모델을 만든다. (대기)
4. `.csv` / `.tsv`: 표 기반 렌더링과 셀 편집 모델을 만든다. (대기)
5. `.yaml` / `.yml`: 주석과 순서 보존이 중요한 구조 텍스트 편집 모델을 만든다. (대기)

## Definition of Done

- [x] 구현된 기능은 원문 모드와 렌더링 모드에서 모두 검증되었다.
- [x] 렌더링 모드의 편집 결과가 원본 텍스트에 안전하게 반영된다.
- [x] 저장 후 다시 열어도 사용자가 의도하지 않은 포맷 변경이 없다.
- [x] 관련 자동 테스트가 있다.
- [x] 자동 테스트로 확인하기 어려운 UI 동작은 수동 검증 체크리스트에 기록되어 있다.
- [x] 보안상 위험한 렌더링이나 외부 요청은 기본적으로 차단된다.
- [x] 색상, 테마, 글꼴, 줄 높이 설정이 렌더링 모드에 반영된다.
- [x] 문서가 현재 구현 상태와 맞다. (최종 완료)
