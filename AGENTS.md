# AGENTS.md - text-pad 작업 규칙

이 문서는 `text-pad` 저장소에서 작업할 때 지켜야 하는 현재 기준만 담는다.
날짜별 변경 문단이나 담당자 표기는 문서에 누적하지 않는다.

## 의사소통

- 사용자에게 프로그램이나 코드를 설명할 때 한국어가 아닌 용어, 변수명, 함수명, 파일명을 처음 언급하면 먼저 뜻을 짧게 풀어쓴다.
- 지시를 그대로 구현하면 충돌, 부작용, 보안 문제가 생길 수 있는 경우 먼저 위험을 말하고 더 적절한 대안을 제시한다.
- 사용자와 프로젝트 문서는 한국어로 작성한다. 단, `docs/features/natural-text-editing.en.md`는 한국어판과 동등한 공개용 영어 문서로 유지한다.

## 문서 구조

- `docs/project-guide.md`: 전체 구조, 현재 기능 범위, 주요 명령.
- `docs/backend-guide.md`: Rust/Tauri 백엔드 계약, 권한, 검증 기준.
- `docs/frontend-guide.md`: SvelteKit 프론트엔드 구조, 설정창, 렌더 모드 기준.
- `docs/features/`: 파일 처리, 렌더 모드, 실행 취소, 설정창, 테마 설정처럼 기능별 세부 계약.
- `docs/features/natural-text-editing.md`, `docs/features/natural-text-editing.en.md`: 렌더 모드의 자연스러운 텍스트 편집 행동 지침 한국어판과 영어판.
- `docs/implementation-checklist.md`: 남은 기능과 완료 기준.

문서는 현재 구현과 앞으로 지켜야 할 계약만 유지한다. 변경 내역은 Git 차이로 확인한다.
글머리, 자동 쌍 문자, 들여쓰기, 줄바꿈, 자동 변환, 캐럿과 선택 영역, 조합 입력, 실행 취소처럼 자연스러운 텍스트 편집에 영향을 주는 기능을 추가, 변경, 제거할 때는 구현과 같은 작업에서 `docs/features/natural-text-editing.md`와 `docs/features/natural-text-editing.en.md`를 반드시 함께 갱신한다. 두 문서의 행동 계약, 섹션 구조, 예제가 서로 어긋난 상태로 남아서는 안 된다.

## 브랜치 운영

- `main` 브랜치는 배포용 브랜치로 유지하며, 기능을 직접 개발하거나 중간 작업을 쌓지 않는다.
- 기능 개발과 기능 통합은 `develop` 브랜치를 기준으로 진행한다.
- 배포할 변경은 검증을 마친 뒤 `develop` 브랜치에서 `main` 브랜치로 반영한다.

## 구현 원칙

- 기존 Tauri 2, SvelteKit, TypeScript, Rust 구조를 우선 따른다.
- 불필요한 추상화보다 가볍고 빠른 데스크톱 편집기 동작을 우선한다.
- 자동 쌍 문자 입력/삭제, Tab/Shift+Tab 들여쓰기, 들여쓰기 공백 백스페이스 삭제, 줄바꿈 시 들여쓰기 유지처럼 사용자의 입력을 가로채 원문을 보정하는 편집 보조 기능은 렌더 모드에서만 동작해야 한다.
- 렌더 모드 전용 편집 보조 기능은 상위 편집 입력 경로에서만 렌더 모드 여부를 판정한다. 들여쓰기, 자동 쌍 문자, 자동 변환 같은 세부 편집 함수는 호출 모드를 직접 확인하지 않고, 호출되면 자기 기능만 수행한다.
- 원문 모드는 HTML `textarea`의 기본 텍스트 편집 동작을 기준으로 하며, 별도 편집 보조 기능으로 공백, 탭, 줄바꿈, 괄호, 따옴표를 바꾸지 않는다.
- 새 편집 기능은 `docs/features/natural-text-editing.md`와 `docs/features/natural-text-editing.en.md`를 먼저 확인한다. 원문 텍스트를 바꾸면 `docs/features/editor-undo.md`도 확인하고, 기능 전체가 하나의 실행 취소 단위로 되돌아가도록 자체 실행 취소 기록에 연결한다.
- Rust 새 코드에서는 `unwrap()`과 `expect()`를 피하고 `Result` 또는 `Option`으로 실패를 처리한다.
- 사용자 파일을 읽거나 쓸 때는 사용자가 선택한 경로와 저장 의도를 기준으로 한다.
- UI 변경은 기존 Windows 메모장형 화면 밀도와 Fluent 스타일을 유지한다.

## 검증

- 프론트엔드 변경 후에는 `npm run check`를 우선 실행한다.
- 프론트엔드, 정적 자산, 설정, Rust 백엔드처럼 앱 내용이 바뀌는 작업은 마지막에 `.agents/skills/text-pad-signed-build/SKILL.md`를 읽고 `npm run tauri:build:signed`를 실행한다.
- 서명 빌드는 종료 코드 0과 새 `text-pad.exe`, MSI, NSIS, 각 업데이터 `.sig`를 모두 확인해야 완료다.
- `npm run tauri build` 직접 실행과 `npm run tauri build -- --no-bundle`은 진단용일 뿐 최종 빌드 근거로 사용하지 않는다.
- 단순 문서나 저장소 지침만 수정한 경우에는 서명 빌드를 생략할 수 있다.
- 개인키, 암호, 복호화 결과, `TAURI_SIGNING_PRIVATE_KEY*` 값을 출력하거나 커밋하지 않고, 기존 업데이터 키를 임의로 재생성하지 않는다.
- 눈에 보이는 프론트엔드 UI 변경과 일반적인 클릭·키 입력은 인앱 브라우저에서 먼저 확인한다. 렌더된 DOM, 좌표, 입력값과 선택 영역, 키보드 동작처럼 브라우저에서 확인 가능한 근거를 우선 사용한다.
- 인앱 브라우저에서 충분히 증명한 프론트엔드 동작을 실제 Tauri 앱에서 동일하게 반복 검증하지 않는다. 서명 빌드 성공과 실제 앱 조작 검증은 서로 다른 기준으로 다룬다.
- 파일 대화상자, 창 제어, 타이틀바 드래그, 운영체제 색상 선택기, 설치·패키징처럼 Tauri 또는 운영체제 연동이 포함된 변경은 실제 앱에서 해당 조작이 끝까지 작동하는지 확인한다.
- 실제 앱에서만 확인할 수 있지만 자동화가 지나치게 느리거나 불안정한 경우에는 무리하게 반복 조작하지 말고, 확인하지 못한 범위를 밝힌 뒤 사용자에게 직접 검증을 요청한다.
- 실제 실행 화면 검증을 못 한 경우에는 완료로 단정하지 말고, 무엇을 확인하지 못했는지 사용자에게 명확히 알린다.
